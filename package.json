{
  "name": "cgr-cas",
  "version": "1.0.0",
  "private": true,
  "description": "Sitio web de CGR Candelario y Asociados — Contadores y Auditores",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "npm run build && npx serve public -l 3000",
    "preview": "npx serve public -l 3000"
  },
  "engines": {
    "node": ">=18"
  }
}
